public static class Constants
{
    public static readonly string TIME_INTERVAL_FORMAT = "F3"; //For displaying time intervals with float.ToString and double.ToString
    public static readonly string SPATIAL_FORMAT = "F2"; //For displaying spatial data (positions, velocities, distances) with float.ToString and double.ToString
}
